events {}

http {
    include       mime.types;
    default_type  application/octet-stream;

    # Suppress [notice] logs
    error_log /var/log/nginx/error.log warn;

    # Filter out healthcheck requests from access logs
    map $http_user_agent $log_ua {
        "Wget" 0;
        default 1;
    }

    access_log /var/log/nginx/access.log combined if=$log_ua;

    server {
        listen 80;
        listen [::]:80;
        root /usr/share/nginx/html;
        index index.html;

        add_header Vary "Accept" always;
        add_header Link '<https://www.gabriellando.com/>; rel="alternate"; type="text/markdown"' always;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
